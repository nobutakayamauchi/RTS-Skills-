skill_id: weekly_dev_report
purpose: generate a reusable weekly development summary
required_packs:
  - dev_pack
outputs_to_rts:
  - execution_record
supported_triggers:
  - manual
  - scheduled
inputs:
  - reporting_period
  - repository_scope
outputs:
  - summary_markdown
  - execution_record
output_format: markdown_summary
summary_scope: repository_activity
